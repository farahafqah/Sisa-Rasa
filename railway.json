{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd src && python run_api.py --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/health"
  }
}